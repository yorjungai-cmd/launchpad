/**
 * Test fixtures and factories.
 * Add typed test data builders here as domain entities are created.
 */

export const fixtures = {
  /** Placeholder — add domain fixtures in later phases */
} as const;
