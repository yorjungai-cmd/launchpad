/**
 * Client-side translation hooks.
 *
 * Re-export next-intl client hooks from a single location.
 * Use in Client Components ("use client").
 * Do NOT use in Server Components — use server.ts instead.
 */
export { useTranslations, useLocale } from "next-intl";
